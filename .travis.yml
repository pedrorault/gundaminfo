dist: focal
language: ruby
services:
  - postgresql
addons:
  postgresql: "11"
  apt:
    packages:
    - postgresql-11
    - postgresql-client-11
before_script:
  - psql -c 'create database travis_ci_test;' -U postgres